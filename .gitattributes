# Git hooks must keep LF line endings. A CRLF shebang ("#!/bin/sh\r") makes a
# POSIX kernel look for an interpreter that does not exist, so a clone would
# silently run no commit gate at all.
.githooks/* text eol=lf
